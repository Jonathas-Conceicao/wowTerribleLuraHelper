---
phase: 04-say-defaults-click-through
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Window.lua
  - Core.lua
  - Config.lua
autonomous: false
requirements:
  - SCAF-13
  - SCAF-14
  - SCAF-15
  - SAFE-06
  - WIN-11
  - WIN-12

must_haves:
  truths:
    - "Fresh install (TerribleLuraHelperDB nil at ADDON_LOADED): db.listenChannels.SAY == true and db.listenChannels.RAID/RAID_LEADER/RAID_WARNING/INSTANCE_CHAT/INSTANCE_CHAT_LEADER all == false; db.macroChannel == \"SAY\"; TLH_* macros target /s."
    - "Upgrade install (v0.1.0 SavedVariables present): every existing channel value preserved exactly; existing db.macroChannel preserved; existing TLH_* macros untouched (no rebuild on upgrade — D-12)."
    - "When db.window.locked == true: win:EnableMouse(false) is in effect; clicks on window/slot/lock-button area pass through to the UI behind."
    - "When db.window.locked == false: win:EnableMouse(true) is in effect; window captures clicks and is draggable (lock/unlock cycle is idempotent — repeatable)."
    - "Repo-wide grep `git grep \"= db\\.\" -- '*.lua' | grep \" or \"` returns zero matches (SAFE-06 gate; the `*.lua` scope filters out planning-doc prose mentions of the anti-pattern)."
    - "Code comment near Core.lua backfill block documents the `if X == nil then` requirement and references SAFE-06 (D-14)."
    - "stylua --check passes on Window.lua, Core.lua, Config.lua (Lua formatting gate per CLAUDE.md)."
  artifacts:
    - path: "Window.lua"
      provides: "applyLockState now toggles win:EnableMouse via lock-state coupling"
      contains: "win:EnableMouse(not locked)"
    - path: "Core.lua"
      provides: "LISTEN_DEFAULTS table + restructured backfill loop + SAY-centric fresh-install defaults + SAFE-06 reminder comment"
      contains: "local LISTEN_DEFAULTS"
    - path: "Config.lua"
      provides: "SAFE-06-compliant table-init for db.listenChannels (`if db.X == nil then db.X = {} end` form, not `or {}`)"
      contains: "if db.listenChannels == nil"
  key_links:
    - from: "Window.lua:applyLockState"
      to: "win frame mouse-enable state"
      via: "EnableMouse(not locked) called after the if/else"
      pattern: "win:EnableMouse\\(not locked\\)"
    - from: "Core.lua ADDON_LOADED handler"
      to: "TerribleLuraHelperDB.listenChannels (fresh + backfill)"
      via: "literal table on first run; LISTEN_DEFAULTS-driven loop on upgrade"
      pattern: "for ch, default in pairs\\(LISTEN_DEFAULTS\\)"
    - from: "Core.lua ADDON_LOADED handler"
      to: "TerribleLuraHelperDB.macroChannel"
      via: "literal default 'SAY' in fresh-install block + nil-guarded backfill"
      pattern: "db\\.macroChannel = \"SAY\""
---

<objective>
Phase 4: SAY Defaults + Click-Through. Two outcomes:

1. **SAY-centric first-run defaults** (SCAF-13, SCAF-14) with safe upgrade backfill (SCAF-15, SAFE-06) — fresh installs get `listenChannels.SAY=true` only, `macroChannel="SAY"`; existing v0.1.0 users keep every channel and macroChannel value untouched.
2. **Click-through when locked** (WIN-11, WIN-12) — locked window is fully pass-through to clicks; unlocked restores full mouse interaction. Implemented via a single `win:EnableMouse(not locked)` line in `applyLockState`.

Purpose: Match real pug/casual usage (SAY is the universally-visible in-instance channel), and prevent the locked helper window from intercepting raid clicks.

Output: Modified Window.lua, Core.lua, Config.lua (one line each for click-through + SAFE-06 fix; ~25 lines of changes in Core.lua for the new defaults + LISTEN_DEFAULTS table + backfill loop restructure + SAFE-06 reminder comment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-say-defaults-click-through/04-CONTEXT.md
@.planning/phases/04-say-defaults-click-through/04-PATTERNS.md
@.planning/research/SUMMARY.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@Window.lua
@Core.lua
@Config.lua

<interfaces>
<!-- Key contracts the executor needs. Extracted from the source files this plan modifies. -->
<!-- Executor should use these directly; do NOT explore further before starting. -->

From Window.lua (current state — lines 205-216, the integration point):
```lua
applyLockState = function()
    local locked = ns.db.window.locked
    if locked then
        win:SetMovable(false)
        win:RegisterForDrag()
        lockBtn:Hide()
    else
        win:SetMovable(true)
        win:RegisterForDrag("LeftButton")
        lockBtn:Show()
    end
end
```
Callers (zero changes needed): CreateWindow (line 174), ns:ToggleLocked (220), ns:LockWindow (265), ns:UnlockWindow (270).

From Core.lua (current state — lines 33-89, fresh-install + backfill block):
```lua
if not TerribleLuraHelperDB then
    TerribleLuraHelperDB = {
        listenChannels = {
            SAY = true,
            RAID = true,            -- ← change to false
            RAID_LEADER = true,     -- ← change to false
            RAID_WARNING = true,    -- ← change to false
            INSTANCE_CHAT = true,   -- ← change to false
            INSTANCE_CHAT_LEADER = true, -- ← change to false
        },
        window = { ... },           -- unchanged
        macroChannel = "RAID",      -- ← change to "SAY"
    }
end
ns.db = TerribleLuraHelperDB

local db = ns.db
if not db.listenChannels then
    db.listenChannels = {}
end
for _, ch in ipairs({ "SAY", "RAID", "RAID_LEADER", "RAID_WARNING", "INSTANCE_CHAT", "INSTANCE_CHAT_LEADER" }) do
    if db.listenChannels[ch] == nil then
        db.listenChannels[ch] = true   -- ← restructure to use LISTEN_DEFAULTS[ch]
    end
end
-- ... db.window backfills (unchanged) ...
if db.macroChannel == nil then
    db.macroChannel = "RAID"           -- ← change to "SAY"
end
```

From Config.lua (current state — line 70, an existing SAFE-06 violation that must be fixed):
```lua
db.listenChannels = db.listenChannels or {}
```
This is the `db.X = db.X or DEFAULT` anti-pattern banned by SAFE-06. Even though it's `or {}` for a table (not a boolean), the regression-grep at PR time (`git grep "= db\." -- '*.lua' | grep " or "`) catches it, and the v1.0.0 ROADMAP success criterion is **zero matches**. Fix in place.

Phase-4-specific constants (to be added to Core.lua):
```lua
local LISTEN_DEFAULTS = {
    SAY = true,
    RAID = false,
    RAID_LEADER = false,
    RAID_WARNING = false,
    INSTANCE_CHAT = false,
    INSTANCE_CHAT_LEADER = false,
}
```
Place as a file-level local near the top of Core.lua, before the `eventFrame` declaration on line 9 — matches the `local NAME = { ... }` convention from Macros.lua (MACROS, CHANNEL_PREFIX) and Window.lua (SLOT_POS, CHAT_EVENTS).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add click-through to Window.lua applyLockState (WIN-11, WIN-12)</name>
  <files>Window.lua</files>
  <read_first>
    - Window.lua (current state — lines 205-216 are the only lines that change; the executor must see exactly the existing applyLockState shape)
    - .planning/phases/04-say-defaults-click-through/04-CONTEXT.md (decisions D-01, D-02, D-03, D-04, D-05 — minimal scope, no defensive iteration, no combat deferral)
    - .planning/phases/04-say-defaults-click-through/04-PATTERNS.md (analog 1 — preferred clean single-line form)
    - .planning/research/STACK.md (CLICK-THRU section — verifies EnableMouse is unrestricted on plain Frame objects)
    - ./CLAUDE.md (hard constraints — confirms this change does not touch chat/macro code paths)
  </read_first>
  <action>
    Modify `Window.lua:applyLockState` (currently lines 205-216) to enable click-through when locked.

    **Exact change** — add ONE line `win:EnableMouse(not locked)` immediately AFTER the closing `end` of the existing if/else block, BEFORE the function's outer `end`. Do NOT modify any line inside the if/else branches. Per D-01, this is the entire click-through code change for Phase 4.

    Target shape (the executor should produce exactly this):
    ```lua
    applyLockState = function()
        local locked = ns.db.window.locked
        if locked then
            win:SetMovable(false)
            win:RegisterForDrag()
            lockBtn:Hide()
        else
            win:SetMovable(true)
            win:RegisterForDrag("LeftButton")
            lockBtn:Show()
        end
        win:EnableMouse(not locked)
    end
    ```

    **Do NOT:**
    - Iterate over slotFrames calling EnableMouse (D-01, D-03 — slot frames don't have mouse enabled today; defensive iteration is out of scope and rejected as speculative future-proofing).
    - Wrap the call in `InCombatLockdown()` or `armRegenRetry` (D-04, D-05 — `EnableMouse` is unrestricted on the plain `CreateFrame("Frame", ...)` window per STACK.md CLICK-THRU; combat deferral is explicitly excluded).
    - Touch lockBtn (already `:Hide()`s when locked, line 210; hidden frames don't intercept clicks per D-02).
    - Add any new exports, callers, or state. `applyLockState` is already invoked by all four entry points (CreateWindow line 174, ToggleLocked 220, LockWindow 265, UnlockWindow 270) — zero call-site changes needed.

    After the edit, run `stylua Window.lua` per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -n "win:EnableMouse(not locked)" Window.lua && stylua --check Window.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "win:EnableMouse(not locked)" Window.lua` returns 1 (exactly one occurrence).
    - The line appears between the closing `end` of the if/else and the closing `end` of `applyLockState` (verified by reading lines 205-218 after edit).
    - `stylua --check Window.lua` exits 0.
    - No new lines added inside the if/else branches (the diff is +1 line outside the branches; the existing 8 in-branch lines are byte-identical).
    - `grep -c "InCombatLockdown" Window.lua` is unchanged from before the edit (no combat-deferral code introduced — `applyLockState` does not gate on combat).
    - `grep -c "slotFrames\[" Window.lua` is unchanged from before the edit (no defensive iteration over slot frames per D-01/D-03).
  </acceptance_criteria>
  <done>
    Window.lua applyLockState calls win:EnableMouse(not locked) once, after the if/else, no other changes; stylua passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Core.lua fresh-install defaults + add LISTEN_DEFAULTS + restructure backfill (SCAF-13, SCAF-14, SCAF-15, SAFE-06)</name>
  <files>Core.lua</files>
  <read_first>
    - Core.lua (current state — lines 1-89; the executor must see the exact existing fresh-install + backfill block before editing)
    - .planning/phases/04-say-defaults-click-through/04-CONTEXT.md (decisions D-09 fresh-install literals, D-10 LISTEN_DEFAULTS + backfill loop, D-11 macroChannel default literal, D-12 NO proactive macro rebuild, D-14 SAFE-06 reminder comment)
    - .planning/phases/04-say-defaults-click-through/04-PATTERNS.md (analogs 2/3/4 — the exact target shapes for the fresh-install block, the LISTEN_DEFAULTS table, the backfill loop, the macroChannel backfill literal)
    - .planning/research/PITFALLS.md (DB-1 — rationale for why `db.X = db.X or DEFAULT` is unsafe for booleans; informs the SAFE-06 reminder comment wording)
    - ./CLAUDE.md (the addon-load workflow — `./scripts/install.bat`, `/reload` test loop)
  </read_first>
  <action>
    Apply four mechanical edits to `Core.lua` in a single pass:

    **Edit 1 — Add `LISTEN_DEFAULTS` constant table near the top of the file.** Insert it as a file-level local AFTER the file header comment block (lines 3-7) and BEFORE the `eventFrame` declaration on line 9 (or wherever the next non-blank line lives). Place a blank line above and below it. Use exactly this content (Lua tab indentation matching the rest of the file):

    ```lua
    -- Per-channel defaults for fresh-install AND backfill (SCAF-13, SCAF-15, SAFE-06).
    -- SAY=true is the v1.0.0 default; all other channels off until user opts in.
    -- Backfill idiom: `if db.X == nil then db.X = DEFAULT end`.
    -- Never `db.X = db.X or DEFAULT` — that clobbers explicit `false`
    -- values (SAFE-06; see .planning/research/PITFALLS.md DB-1).
    local LISTEN_DEFAULTS = {
    	SAY = true,
    	RAID = false,
    	RAID_LEADER = false,
    	RAID_WARNING = false,
    	INSTANCE_CHAT = false,
    	INSTANCE_CHAT_LEADER = false,
    }
    ```

    The 5-line comment block above the table satisfies D-14 (SAFE-06 reminder near the backfill block — the LISTEN_DEFAULTS table IS used by the backfill block, so co-locating the comment with the table is the natural placement).

    **Edit 2 — Update the fresh-install literal block (currently Core.lua lines 33-53).** Change five `listenChannels.<X> = true` lines to `false`, and the `macroChannel = "RAID"` literal to `"SAY"`. The `SAY = true` line and the `window` sub-table are byte-identical (do not modify). Target shape after edit:

    ```lua
    if not TerribleLuraHelperDB then
    	TerribleLuraHelperDB = {
    		listenChannels = {
    			SAY = true,
    			RAID = false,
    			RAID_LEADER = false,
    			RAID_WARNING = false,
    			INSTANCE_CHAT = false,
    			INSTANCE_CHAT_LEADER = false,
    		},
    		window = {
    			scale = 1.00,
    			locked = false,
    			autoHide = false,
    			position = nil,
    			alpha = 1.00,
    			visible = false,
    		},
    		macroChannel = "SAY",
    	}
    end
    ```

    **Edit 3 — Restructure the listenChannels backfill loop (currently Core.lua lines 61-65).** Replace the hardcoded `ipairs({...})` + `db.listenChannels[ch] = true` form with a `pairs(LISTEN_DEFAULTS)` form that uses each channel's own default. Preserve the `if db.listenChannels[ch] == nil then` guard verbatim. Target shape after edit:

    ```lua
    for ch, default in pairs(LISTEN_DEFAULTS) do
    	if db.listenChannels[ch] == nil then
    		db.listenChannels[ch] = default
    	end
    end
    ```

    This preserves SCAF-15 semantics — existing user values are never overwritten (the `== nil` guard is unchanged). On a v0.1.0 → v1.0.0 upgrade, all six channels are already populated, so the loop is a no-op for upgraders. On a fresh install this branch never runs (the fresh-install literal in Edit 2 wrote everything already).

    **Edit 4 — Update the macroChannel backfill literal (currently Core.lua lines 87-89).** Change `db.macroChannel = "RAID"` to `db.macroChannel = "SAY"`. Preserve the `if db.macroChannel == nil then` guard verbatim. Target shape:

    ```lua
    if db.macroChannel == nil then
    	db.macroChannel = "SAY"
    end
    ```

    **Per D-12: do NOT add a proactive `ns:RegisterMacros()` call on upgrade.** Existing users have working macros bound to action bars; rebuilding silently could fail mid-combat or overwrite user customizations. Fresh installs hit `ns:InitMacros()` from the existing PLAYER_LOGIN handler.

    **Per D-07: do NOT add any chat print, `db.notifiedV1` flag, or `db.schemaVersion` field.** The upgrade is silent.

    After all four edits, run `stylua Core.lua` per CLAUDE.md.
  </action>
  <verify>
    <automated>grep -c "local LISTEN_DEFAULTS" Core.lua | grep -q "^1$" && grep -c "for ch, default in pairs(LISTEN_DEFAULTS) do" Core.lua | grep -q "^1$" && grep -c 'macroChannel = "SAY"' Core.lua | grep -q "^2$" && stylua --check Core.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "local LISTEN_DEFAULTS" Core.lua` returns 1 (the new constant table is declared exactly once).
    - `grep -c "RAID = false" Core.lua` returns 2 (one in LISTEN_DEFAULTS, one in fresh-install literal).
    - `grep -c "RAID_LEADER = false" Core.lua` returns 2.
    - `grep -c "RAID_WARNING = false" Core.lua` returns 2.
    - `grep -c "INSTANCE_CHAT = false" Core.lua` returns 2.
    - `grep -c "INSTANCE_CHAT_LEADER = false" Core.lua` returns 2.
    - `grep -c "SAY = true" Core.lua` returns 2 (one in LISTEN_DEFAULTS, one in fresh-install literal).
    - `grep -c "macroChannel = \"SAY\"" Core.lua` returns exactly 2 (fresh-install literal at line ~51 region + backfill literal at line ~88 region).
    - `grep -c "macroChannel = \"RAID\"" Core.lua` returns 0 (the old default is fully removed).
    - `grep -c "for ch, default in pairs(LISTEN_DEFAULTS) do" Core.lua` returns 1 (the restructured backfill loop is present).
    - `grep -c "ipairs({ \"SAY\", \"RAID\"" Core.lua` returns 0 (the old hardcoded ipairs loop is gone).
    - `grep -c "SAFE-06" Core.lua` returns at least 1 (the reminder comment references SAFE-06 explicitly per D-14).
    - The reminder comment is co-located with `LISTEN_DEFAULTS` (within 5 lines above the `local LISTEN_DEFAULTS = {` line) — verified by reading the section after edit.
    - `grep -c "if db.macroChannel == nil then" Core.lua` returns 1 (the existing nil-guard is preserved verbatim).
    - `grep -c "RegisterMacros\\|InitMacros" Core.lua` is unchanged from before the edit (no proactive macro rebuild added per D-12).
    - `grep -c "notifiedV1\\|schemaVersion" Core.lua` returns 0 (no schema-versioning scaffold per D-07/D-08).
    - `stylua --check Core.lua` exits 0.
  </acceptance_criteria>
  <done>
    Core.lua has LISTEN_DEFAULTS table, SAY-centric fresh-install defaults, restructured backfill loop using LISTEN_DEFAULTS, "SAY" macroChannel default in both fresh-install + backfill paths, and a SAFE-06 reminder comment co-located with LISTEN_DEFAULTS; stylua passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Fix the existing SAFE-06 violation in Config.lua line 70</name>
  <files>Config.lua</files>
  <read_first>
    - Config.lua (current state — line 70 contains the violation; the executor must see the surrounding `RegisterChannelToggles` function context, lines 65-90)
    - .planning/phases/04-say-defaults-click-through/04-CONTEXT.md (D-13 — manual grep at PR review time is the gate; SAFE-06 must return zero matches)
    - .planning/research/PITFALLS.md (DB-1 — rationale for the anti-pattern; even for tables, `or {}` matches the regression-grep)
    - .planning/REQUIREMENTS.md (SAFE-06 full text — "No usage of `db.X = db.X or DEFAULT` pattern anywhere in the addon")
  </read_first>
  <action>
    Replace the existing `db.X = db.X or DEFAULT` anti-pattern in Config.lua with the SAFE-06-compliant `if db.X == nil then` form.

    **Current state** (`Config.lua` line 70, inside `RegisterChannelToggles`):
    ```lua
    db.listenChannels = db.listenChannels or {}
    ```

    **Required change** — replace with:
    ```lua
    if db.listenChannels == nil then
    	db.listenChannels = {}
    end
    ```

    **Rationale:** The v1.0.0 ROADMAP success criterion 3 for Phase 4 reads: "A grep of the codebase finds zero instances of the `db.X = db.X or DEFAULT` anti-pattern; every backfill path uses `if db.X == nil then db.X = DEFAULT end`." The current `or {}` form, while semantically defensive (a non-nil table evaluates truthy in Lua, so the `or` doesn't actually clobber here), still matches the regression-grep `git grep "= db\." -- '*.lua' | grep " or "`. Note the `-- '*.lua'` scope filter — it restricts the grep to source files and intentionally excludes planning-doc prose mentions of the anti-pattern (in PITFALLS.md, PATTERNS.md, CONTEXT.md, REQUIREMENTS.md). Without that scope, the grep returns false-positive matches against documentation that *describes* the banned pattern (the very docs that warn against it). Fixing the Config.lua line now satisfies the success criterion verbatim AND removes the only nearby example future contributors might copy-paste, propagating the anti-pattern.

    Do NOT modify any other line in Config.lua. Do NOT touch the per-channel `Settings.RegisterAddOnSetting` block immediately below (lines 71-87) — those lines are unchanged.

    After the edit, run `stylua Config.lua` per CLAUDE.md.
  </action>
  <verify>
    <automated>git grep "= db\." -- '*.lua' | grep " or " ; test $? -eq 1 && stylua --check Config.lua</automated>
  </verify>
  <acceptance_criteria>
    - `git grep "= db\." -- '*.lua' | grep " or "` returns zero matches across the entire repository (i.e. exit code 1 — `grep` exits non-zero when no lines match). This is the SAFE-06 regression gate.
    - `grep -n "db.listenChannels = db.listenChannels or" Config.lua` returns no matches.
    - `grep -n "if db.listenChannels == nil then" Config.lua` returns exactly one match.
    - `grep -c "db.listenChannels =" Config.lua` returns 1 (only the assignment inside the if-nil block).
    - The lines below the edit (the `for _, ch in ipairs(CHANNELS) do` loop on what was line 71 and is still ~line 72-73 after edit) are byte-identical to before.
    - `stylua --check Config.lua` exits 0.
  </acceptance_criteria>
  <done>
    Config.lua line 70 uses `if db.listenChannels == nil then ... end` instead of `db.listenChannels = db.listenChannels or {}`; repo-wide SAFE-06 grep returns zero matches; stylua passes.
  </done>
</task>

<task type="checkpoint:automated">
  <name>Task 4: Final automated verification gate (SAFE-06 + stylua + addon-loads sanity)</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/REQUIREMENTS.md (the six Phase 4 REQ IDs — full text)
    - .planning/ROADMAP.md (Phase 4 success criteria 1-5)
    - .planning/phases/04-say-defaults-click-through/04-CONTEXT.md (D-13 manual grep at PR review time)
    - ./CLAUDE.md (stylua-after-task gate, install.bat for in-game test, milestone branch invariant)
  </read_first>
  <action>
    Run the full automated verification suite and present results. This task is a gate before the human-verify UAT in Task 5.

    Execute the following verifications in order. If any fails, stop and surface the failure to the user — do NOT proceed to Task 5 until every check passes.

    1. **SAFE-06 regression grep** (D-13 / SAFE-06 / ROADMAP success criterion 3):
       ```
       git grep "= db\." -- '*.lua' | grep " or "
       ```
       Expected: zero matches (grep exits 1).

    2. **Stylua format check** on all three modified files:
       ```
       stylua --check Window.lua Core.lua Config.lua
       ```
       Expected: exit 0.

    3. **No accidental SendChatMessage / msg-indexing introductions** (CLAUDE.md hard constraints — sanity check that this phase didn't drift):
       ```
       grep -n "SendChatMessage" Window.lua Core.lua Config.lua Macros.lua
       ```
       Expected: zero matches.

    4. **No accidental COMBAT_LOG_EVENT_UNFILTERED** (CLAUDE.md hard constraint — sanity check):
       ```
       grep -n "COMBAT_LOG_EVENT_UNFILTERED" Window.lua Core.lua Config.lua Macros.lua
       ```
       Expected: zero matches.

    5. **Phase-specific code presence checks** (consolidated from Tasks 1-3):
       ```
       grep -c "win:EnableMouse(not locked)" Window.lua          # expect 1
       grep -c "local LISTEN_DEFAULTS" Core.lua                  # expect 1
       grep -c "for ch, default in pairs(LISTEN_DEFAULTS) do" Core.lua   # expect 1
       grep -c 'macroChannel = "SAY"' Core.lua                   # expect 2
       grep -c 'macroChannel = "RAID"' Core.lua                  # expect 0
       grep -c "if db.listenChannels == nil then" Config.lua     # expect 1
       grep -c "SAFE-06" Core.lua                                # expect >= 1
       ```

    6. **Install to WoW addons folder** so Task 5's UAT has the new code in place. Per `feedback_install_for_testing` user memory, ALWAYS run install.bat before asking the user to verify in-game — do NOT offer it as a choice:
       ```
       ./scripts/install.bat
       ```
       Expected: exit 0; the script copies the addon to the live WoW Retail addons folder.

    7. **Confirm milestone branch invariant** (CLAUDE.md workflow rule — all v1.0.0 work on `milestone/1.0.0`, never directly on `main`):
       ```
       git branch --show-current
       ```
       Expected: `milestone/1.0.0` (the active milestone branch). If this prints `main`, stop and surface the error — committing to `main` violates the workflow rule.

    Print a concise summary of the seven checks before handing off to Task 5.
  </action>
  <verify>
    <automated>./scripts/install.bat && bash -c 'set -e; ! (git grep "= db\." -- '*.lua' | grep " or "); stylua --check Window.lua Core.lua Config.lua; ! grep -q "SendChatMessage" Window.lua Core.lua Config.lua Macros.lua; ! grep -q "COMBAT_LOG_EVENT_UNFILTERED" Window.lua Core.lua Config.lua Macros.lua; [ "$(grep -c "win:EnableMouse(not locked)" Window.lua)" = "1" ]; [ "$(grep -c "local LISTEN_DEFAULTS" Core.lua)" = "1" ]; [ "$(grep -c "for ch, default in pairs(LISTEN_DEFAULTS) do" Core.lua)" = "1" ]; [ "$(grep -c "macroChannel = \"SAY\"" Core.lua)" = "2" ]; [ "$(grep -c "macroChannel = \"RAID\"" Core.lua)" = "0" ]; [ "$(grep -c "if db.listenChannels == nil then" Config.lua)" = "1" ]; [ "$(git branch --show-current)" = "milestone/1.0.0" ]'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep "= db\." -- '*.lua' | grep " or "` returns 0 matches (exit code 1 from the inner grep).
    - `stylua --check Window.lua Core.lua Config.lua` exits 0.
    - No `SendChatMessage` references in Window.lua/Core.lua/Config.lua/Macros.lua (CLAUDE.md hard constraint).
    - No `COMBAT_LOG_EVENT_UNFILTERED` references in Window.lua/Core.lua/Config.lua/Macros.lua (CLAUDE.md hard constraint).
    - All seven phase-specific code-presence grep counts match the expected values above (exact integers, not "looks right").
    - `./scripts/install.bat` exits 0 (addon files copied to live WoW Retail addons folder so Task 5's in-game UAT runs against the new build).
    - `git branch --show-current` prints `milestone/1.0.0` (workflow invariant).
  </acceptance_criteria>
  <done>
    All seven automated checks green; install.bat has copied the new build to WoW Retail; ready for Task 5 in-game UAT.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: In-game UAT — three D-15 checkpoints (fresh install / upgrade / click-through)</name>
  <files>(no files modified — human verification only; the addon was installed by Task 4 step 6)</files>
  <read_first>
    - .planning/phases/04-say-defaults-click-through/04-CONTEXT.md (D-15 — the three explicit UAT checkpoints; D-06 — the in-combat lock/unlock gate)
    - .planning/REQUIREMENTS.md (SCAF-13, SCAF-14, SCAF-15, WIN-11, WIN-12 full text — the criteria the human is verifying)
    - .planning/ROADMAP.md (Phase 4 success criteria 1, 2, 4, 5 — same criteria, narrative form)
  </read_first>
  <action>
    **This is a human-verify checkpoint.** Claude does NOT execute any commands here. Claude's role is to:

    1. Confirm Task 4 reported PASS on all seven automated checks (especially that `./scripts/install.bat` completed successfully — the addon must be installed in the live WoW Retail folder before the user can test).
    2. Present the three D-15 checkpoints from `<how-to-verify>` below to the user, verbatim.
    3. Wait for the user's response on `<resume-signal>`. Do NOT mark Phase 4 done, do NOT write the SUMMARY, and do NOT commit until the user types "approved".
    4. If the user reports a failure on Checkpoint 3 step k (in-combat `ADDON_ACTION_BLOCKED`), surface this as a Phase 4.x follow-up: per CONTEXT.md D-06, the fallback is to wrap the new `win:EnableMouse(not locked)` call in `InCombatLockdown()` + `armRegenRetry` mirroring `Macros.lua` lines 86-98. Do NOT silently patch — surface to the user.
    5. If the user reports a failure on any other checkpoint, surface the specific step that failed and what they observed; do NOT attempt to "fix" autonomously without re-planning.
  </action>
  <what-built>
    The Phase 4 code changes are complete and installed to the live WoW Retail addons folder by Task 4. Three behaviors need human verification in-game per CONTEXT.md D-15:
      1. Fresh installs get SAY-only listenChannels and macroChannel="SAY" — TLH_* macros target /s.
      2. Upgrades from v0.1.0 SavedVariables preserve every existing channel value and the existing macroChannel — existing TLH_* macros are NOT rebuilt.
      3. Locked window is fully click-through; unlocked window captures clicks and is draggable; lock/unlock is idempotent across 3+ cycles, including in combat.
  </what-built>
  <how-to-verify>
    **Run all three checkpoints in order. Report PASS / FAIL for each, with notes if anything deviates.**

    **Checkpoint 1 — Fresh-install test** (validates SCAF-13, SCAF-14):
      a. Fully exit WoW (not just `/reload` — the SavedVariables file is only flushed to disk on logout/exit).
      b. Delete the file `WTF/Account/<your-account>/SavedVariables/TerribleLuraHelperDB.lua` (or rename it to `.bak` to preserve a backup).
      c. Launch WoW, log into any character.
      d. Confirm the addon banner prints `TerribleLuraHelper loaded.` with no Lua errors.
      e. Type `/reload` (this flushes the freshly-initialized DB to disk so we can inspect it).
      f. Open `WTF/Account/<your-account>/SavedVariables/TerribleLuraHelperDB.lua` in a text editor.
      g. Confirm `listenChannels = { SAY = true, RAID = false, RAID_LEADER = false, RAID_WARNING = false, INSTANCE_CHAT = false, INSTANCE_CHAT_LEADER = false }` (only SAY is true; all five other channels are false).
      h. Confirm `macroChannel = "SAY"`.
      i. In WoW, open `/macro` and verify the five `TLH_*` macros (TLH_Diamond / TLH_Triangle / TLH_Circle / TLH_Cross / TLH_T) exist and their bodies start with `/s ` (slash + lowercase s + space) — NOT `/raid `.

      Expected: every check above is true.

    **Checkpoint 2 — Upgrade preserve test** (validates SCAF-15):
      a. Fully exit WoW.
      b. Restore (or hand-craft, if step 1's `.bak` isn't usable) a v0.1.0-shaped `TerribleLuraHelperDB.lua` with all-channels-true (`SAY=true, RAID=true, RAID_LEADER=true, RAID_WARNING=true, INSTANCE_CHAT=true, INSTANCE_CHAT_LEADER=true`) and `macroChannel = "RAID"`. (Hand-craft option: copy the file from before Phase 4 install; or if you have a fresh DB from checkpoint 1, hand-edit the values to all `true` and macroChannel="RAID" before launching.)
      c. Launch WoW, log into any character.
      d. Confirm no Lua errors at load.
      e. `/reload` to flush state.
      f. Open `TerribleLuraHelperDB.lua` again.
      g. Confirm every channel is STILL `true` (none flipped to `false` — this is the SCAF-15 SAFE-06 backfill safety check).
      h. Confirm `macroChannel` is STILL `"RAID"` (the existing value is preserved by the `if == nil` guard).
      i. Open `/macro` and confirm the five existing TLH_* macros' bodies still start with `/raid ` — they were NOT rebuilt to `/s ` (per D-12, no proactive macro rebuild on upgrade).

      Expected: every existing value is preserved exactly.

    **Checkpoint 3 — Click-through cycle test** (validates WIN-11, WIN-12 + D-06 in-combat ADDON_ACTION_BLOCKED gate):
      a. Type `/lura show`. The smile-arc helper window opens.
      b. Drag the window so it covers an action-bar slot you can identify (e.g., a slot bound to a vendor item or trinket — something visible).
      c. Confirm the window is currently UNLOCKED (the on-window "Lock" text in the bottom-right corner is visible). If the window is already locked, type `/lura unlock` first.
      d. Click on the smile-arc area covering the action-bar slot. Confirm the window CAPTURES the click (the action-bar slot is NOT triggered) — this is the unlocked-default WIN-12 behavior.
      e. Type `/lura lock`. The "Lock" text disappears.
      f. Click on the same area. Confirm the click PASSES THROUGH to the action-bar slot (the bound action triggers — vendor item used, trinket fires, etc.) — this is the WIN-11 click-through behavior.
      g. Click in three different positions over the locked window: top of the smile-arc, middle (BOSS label area), and the slot frames themselves. Each click must pass through.
      h. Type `/lura unlock`. The "Lock" text reappears.
      i. Click on the smile-arc area again. Confirm the window CAPTURES the click again (no action-bar trigger) — confirms unlocked behavior is restored.
      j. Repeat the lock-click-unlock-click cycle two more times (total of 3+ cycles) — confirm the behavior is idempotent across cycles.
      k. **D-06 combat-lockdown gate:** With the window unlocked, target a training dummy (or pull a real boss for a hardcore test) and engage combat. While in combat, type `/lura lock`. Confirm:
         - No `ADDON_ACTION_BLOCKED` error pops up in the chat frame.
         - No Lua error pops up.
         - The "Lock" text disappears (lock state successfully applied).
         - Click on the locked window → click passes through to the action bar beneath. Click-through works while in combat.
         - Type `/lura unlock` while still in combat — same expectations: no error, mouse interaction restored, no taint warnings.

      Expected: every cycle behaves as specified; in-combat lock/unlock works without ADDON_ACTION_BLOCKED.

    **If any checkpoint fails:** Report which step failed and what the observed behavior was. Do NOT type "approved" until every checkpoint passes — the planner will revise the plan or bump the EnableMouse call into a combat-deferral path (the D-06 fallback noted in CONTEXT.md).
  </how-to-verify>
  <verify>
    <automated>echo "Human verification required - see how-to-verify block. Resume on user approval signal."</automated>
  </verify>
  <acceptance_criteria>
    - Checkpoint 1 PASS — user confirms in-game inspection of `TerribleLuraHelperDB.lua` after fresh-install + `/reload` shows: `listenChannels.SAY=true`, all five other channels `=false`, `macroChannel="SAY"`, and `/macro` lists the five TLH_* macros with `/s `-prefixed bodies.
    - Checkpoint 2 PASS — user confirms in-game inspection after upgrade-test load + `/reload` shows every original channel value preserved (still `true` if originally `true`), `macroChannel` still `"RAID"`, and existing TLH_* macro bodies unchanged (still `/raid `-prefixed; not rebuilt).
    - Checkpoint 3 PASS — user confirms (1) unlocked window captures clicks (action-bar slot under it does NOT trigger), (2) locked window passes clicks through (action-bar slot DOES trigger), (3) the lock-click-unlock-click cycle is repeatable 3+ times with consistent behavior, (4) `/lura lock` and `/lura unlock` issued during live combat produce zero `ADDON_ACTION_BLOCKED` errors and zero Lua errors.
    - User explicitly types "approved" in their reply, OR describes specifically which checkpoint/step failed and what they observed.
  </acceptance_criteria>
  <done>
    All three D-15 UAT checkpoints PASS; user has typed "approved"; no in-combat ADDON_ACTION_BLOCKED errors observed; phase ready for SUMMARY + commit.
  </done>
  <resume-signal>Type "approved" when all three D-15 checkpoints pass, OR describe specifically what failed (which step, what you saw vs. what was expected).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → SavedVariables file | The `TerribleLuraHelperDB.lua` file on disk can be hand-edited by the user between game sessions; values arrive at `ADDON_LOADED` already deserialized into Lua tables, but type/value contents are user-controlled. |
| Window frame ↔ UIParent layer | When `EnableMouse(false)` is called on the window, the click event propagates up the parent chain to UIParent's children stacked behind the window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `TerribleLuraHelperDB.macroChannel` (string) hand-edited to a value not in `CHANNEL_PREFIX` | accept | Existing defense in `Macros.lua` line 53 (`CHANNEL_PREFIX[ns.db.macroChannel] or "/raid"`) handles bad values gracefully — falls back to `/raid` body. Phase 4 changes the default literal but does NOT change this fallback path. No new code needed. |
| T-04-02 | Tampering | `TerribleLuraHelperDB.listenChannels.X` hand-edited to non-boolean | accept | The `if db.X == nil then` backfill guard only matters for missing keys; non-boolean truthy values still evaluate as the channel filter dictates (`if not ns.db.listenChannels[event:sub(10)] then return end` in `Window.lua` chatFrame handler). Worst case: a misconfigured channel listens or doesn't listen. No security impact. |
| T-04-03 | Denial of Service | `EnableMouse(false)` interferes with Blizzard error overlays | accept | Verified: Blizzard error overlays (`UIErrorsFrame`, `StaticPopup_Show`, `Lua error frames`) are top-level UIParent children, NOT parented to `TerribleLuraHelperWindow`. The window's mouse-disabled state does not block error UIs. Standard WoW addon UI layering applies. |
| T-04-04 | Repudiation | Code review at PR time misses a future regression where a contributor reintroduces `db.X = db.X or DEFAULT` | mitigate | (a) SAFE-06 reminder comment in `Core.lua` near `LISTEN_DEFAULTS` (D-14, Task 2). (b) Manual grep at PR review: `git grep "= db\." -- '*.lua' \| grep " or "` must return zero matches (D-13, Task 4 acceptance criterion). (c) Task 3 fixes the only pre-existing violation in Config.lua so the grep is a true zero-or-fail gate, not a known-noise filter. No CI/pre-commit automation per D-13 (deferred — addon scale doesn't warrant). |
| T-04-05 | Elevation of Privilege | `EnableMouse(false)` called from non-secure code in combat triggers `ADDON_ACTION_BLOCKED` | mitigate | STACK.md verified that `EnableMouse` is unrestricted on plain `CreateFrame("Frame", ...)` objects — `TerribleLuraHelperWindow` is exactly that. D-04/D-05 explicitly excluded combat-deferral. **Verification:** Task 5 Checkpoint 3 step k tests `/lura lock` and `/lura unlock` during live combat against a training dummy or boss, confirming zero `ADDON_ACTION_BLOCKED` errors. **Fallback:** If the in-combat test surfaces a problem, follow-up patch wraps `EnableMouse` in `InCombatLockdown()` + `armRegenRetry` (mirror Macros.lua lines 86-98). Per D-06 this becomes a Phase 4.x or v1.0.1 patch. |

**Severity assessment:** All threats are LOW. The phase changes a frame property (`EnableMouse`) and a handful of literal default values. No chat handling, no network, no auth, no protected APIs. Phase ships once Task 5's three D-15 checkpoints pass.
</threat_model>

<verification>
**Automated (Task 4):**
- SAFE-06 regression grep: `git grep "= db\." -- '*.lua' | grep " or "` → zero matches.
- Stylua format check: `stylua --check Window.lua Core.lua Config.lua` → exit 0.
- Hard-constraint sanity: no `SendChatMessage`, no `COMBAT_LOG_EVENT_UNFILTERED` introduced in any file.
- All seven phase-specific code-presence grep counts match.
- `./scripts/install.bat` exits 0; addon copied to live WoW Retail folder.
- Active git branch is `milestone/1.0.0`.

**Human-verify (Task 5):**
- Checkpoint 1: Fresh install yields SAY-only listenChannels, macroChannel="SAY", `/s`-bodied macros.
- Checkpoint 2: Upgrade preserves all existing channel values, macroChannel value, and existing macro bodies.
- Checkpoint 3: Locked window is fully click-through; unlocked captures clicks; 3+ lock/unlock cycles are idempotent; in-combat lock/unlock produces zero `ADDON_ACTION_BLOCKED` errors.
</verification>

<success_criteria>
**Phase 4 ships (squash-ready) when all of the below are TRUE:**

1. **SCAF-13** — Fresh-install `TerribleLuraHelperDB.listenChannels` has exactly one true (`SAY`); all five other channels are `false`. (Task 5 Checkpoint 1.)
2. **SCAF-14** — Fresh-install `TerribleLuraHelperDB.macroChannel == "SAY"`; the five TLH_* macros have bodies starting with `/s `. (Task 5 Checkpoint 1.)
3. **SCAF-15** — Upgrading over a v0.1.0 SavedVariables file with custom channel choices (e.g. all-true + macroChannel=RAID) preserves every existing value; the existing TLH_* macros are not rebuilt. (Task 5 Checkpoint 2.)
4. **SAFE-06** — `git grep "= db\." -- '*.lua' | grep " or "` returns zero matches across the entire repository (Task 4); SAFE-06 reminder comment is present in Core.lua near LISTEN_DEFAULTS (Task 2 acceptance criteria).
5. **WIN-11** — When `db.window.locked == true`, clicks on every region of the helper window pass through to UI elements behind it (Task 5 Checkpoint 3 steps e-g).
6. **WIN-12** — When `db.window.locked == false`, mouse interaction is restored — clicks are captured and the window can be dragged; 3+ lock/unlock cycles are idempotent (Task 5 Checkpoint 3 steps c-d, h-j).
7. **D-06 combat gate** — Pressing `/lura lock` and `/lura unlock` during a live combat encounter produces zero `ADDON_ACTION_BLOCKED` errors (Task 5 Checkpoint 3 step k).
8. **CLAUDE.md gates** — stylua passes on all modified files; no `SendChatMessage`, no `COMBAT_LOG_EVENT_UNFILTERED` introduced; commit lands on `milestone/1.0.0`, not `main`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-say-defaults-click-through/04-01-SUMMARY.md` documenting:

- Final diff summary across Window.lua, Core.lua, Config.lua (one-liner per change).
- Actual `LISTEN_DEFAULTS` table contents written.
- Confirmation that all 6 phase REQ IDs are satisfied (SCAF-13, SCAF-14, SCAF-15, SAFE-06, WIN-11, WIN-12).
- Task 5 UAT results: PASS/FAIL for each of the three D-15 checkpoints, with notes on any deviation.
- Whether the D-06 in-combat lock/unlock test surfaced any `ADDON_ACTION_BLOCKED` errors. If yes, document the failure mode and recommend a Phase 4.x combat-deferral patch.
- Stylua + git-branch + SAFE-06 grep verification outputs.
- Carry-forward decisions (if any) for Phase 5 and Phase 6.
</output>
