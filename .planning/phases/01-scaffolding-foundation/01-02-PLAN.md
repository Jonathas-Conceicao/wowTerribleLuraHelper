---
phase: 01-scaffolding-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - TerribleLuraHelper.toc
  - Core.lua
  - Macros.lua
  - Window.lua
  - Config.lua
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02
  - SCAF-03
must_haves:
  truths:
    - "TerribleLuraHelper.toc declares Interface 120005, Title TerribleLuraHelper, Author Jonathas-Conceicao, Version @project-version@, X-Curse-Project-ID 1529832, X-Wago-ID XKqArdKy, SavedVariables TerribleLuraHelperDB"
    - "TerribleLuraHelper.toc lists exactly four Lua files in order: Core.lua, Macros.lua, Window.lua, Config.lua"
    - "Every Lua file's first line is `local addonName, ns = ...` (D-01 namespace pattern)"
    - "Core.lua's ADDON_LOADED handler initializes TerribleLuraHelperDB to the grouped schema in D-03 (enabled=false; listenChannels with 6 keys all true; window={scale=1.00, locked=true, autoHide=false, position=nil}; sequence={}) and backfills missing keys on existing DBs"
    - "Core.lua's ADDON_LOADED handler calls ns:InitMacros(), ns:InitWindow(), ns:InitConfig() (the dispatcher hooks Phase 2 and Phase 3 code into)"
    - "Core.lua prints a colored `TerribleLuraHelper loaded.` banner on first ADDON_LOADED matching the addonName"
    - "Macros.lua, Window.lua, Config.lua define the matching empty-body Init functions (ns.InitMacros = function() end style) — Phase 2/3 fill these in"
    - "stylua succeeds without changes on all four Lua files (defaults; no .stylua.toml per D-13)"
  artifacts:
    - path: "TerribleLuraHelper.toc"
      provides: "Addon manifest"
      contains: "## Interface: 120005"
    - path: "Core.lua"
      provides: "Namespace, SavedVariables init, init dispatcher, load banner"
      min_lines: 60
      contains: "if not TerribleLuraHelperDB then"
    - path: "Macros.lua"
      provides: "Namespace + ns.InitMacros stub (Phase 2 fills)"
      min_lines: 5
    - path: "Window.lua"
      provides: "Namespace + ns.InitWindow stub (Phase 2 fills)"
      min_lines: 5
    - path: "Config.lua"
      provides: "Namespace + ns.InitConfig stub (Phase 3 fills)"
      min_lines: 5
  key_links:
    - from: "TerribleLuraHelper.toc"
      to: "Core.lua, Macros.lua, Window.lua, Config.lua"
      via: "load-order list (Core.lua first)"
      pattern: "^Core\\.lua$"
    - from: "Core.lua ADDON_LOADED handler"
      to: "TerribleLuraHelperDB"
      via: "if not TerribleLuraHelperDB then ... end + backfill loop"
      pattern: "TerribleLuraHelperDB"
    - from: "Core.lua ADDON_LOADED handler"
      to: "ns:InitMacros / ns:InitWindow / ns:InitConfig"
      via: "dispatcher call after DB init"
      pattern: "ns:Init(Macros|Window|Config)"
---

<objective>
Land the addon code skeleton: the `.toc` manifest and the four-file Lua split (Core / Macros / Window / Config) per D-01. Core.lua holds the SavedVariables init + backfill (D-03 grouped schema), the load banner, and the dispatcher that calls the (empty) Init functions in the other three files. The other three files are namespace-correct stubs ready for Phase 2 (Macros, Window) and Phase 3 (Config) to fill in.

Purpose: This is the "addon loads as a no-op and prints its load banner" payload from the phase goal — the first piece of the codebase that actually shows up inside WoW. The grouped DB schema must be exact (D-03) because Phase 2 and Phase 3 read keys directly from it; getting the schema wrong here cascades. The four-file split per D-01 lands now (not later) so Phase 2/3 don't have to do a "split" sub-task; they just fill function bodies.

Output: `TerribleLuraHelper.toc` + `Core.lua` (real implementation) + `Macros.lua`, `Window.lua`, `Config.lua` (stubs with namespace + empty Init function). Addon loads in WoW with no errors and prints the banner; `TerribleLuraHelperDB` appears in SavedVariables after a `/reload` round-trip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffolding-foundation/01-CONTEXT.md
@CLAUDE.md

<interfaces>
TBT's Core.lua SavedVariables-init pattern (lines 1-90) is the source-of-truth.
Adapt: TerribleBuffTrackerDB to TerribleLuraHelperDB; trackedBuffs/containerSettings schema to grouped TLH schema in D-03.
The ns.* dispatcher pattern is new for TLH (TBT calls ns:InitBuffEngine() directly; TLH has 3 init points).

TBT Core.lua first-line + namespace pattern (verbatim, line 1):

    local addonName, ns = ...

TBT Core.lua's DB-init shape (lines 14-26 — adapt for TLH):

    if event == "ADDON_LOADED" then
        local name = ...
        if name ~= addonName then return end
        if not TerribleBuffTrackerDB then TerribleBuffTrackerDB = { ... } end
        ns.db = TerribleBuffTrackerDB
        -- backfill loop here

TBT Core.lua's load-banner format (lines 87-89 — adapt color/text for TLH):

    print("|cff00ccffTerribleBuffTracker|r loaded. Type |cff00ff00/tbt|r ...")

TLH grouped DB schema (D-03, exact):

    TerribleLuraHelperDB = {
        enabled = false,
        listenChannels = {
            SAY = true,
            RAID = true,
            RAID_LEADER = true,
            RAID_WARNING = true,
            INSTANCE_CHAT = true,
            INSTANCE_CHAT_LEADER = true,
        },
        window = {
            scale = 1.00,
            locked = true,
            autoHide = false,
            position = nil,
        },
        sequence = {},
    }
</interfaces>

<dependency_note>
Plan 01-01 must have completed: working tree must be on `milestone/0.1.0`. Confirm with `git rev-parse --abbrev-ref HEAD` before starting.
</dependency_note>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write TerribleLuraHelper.toc</name>
  <files>TerribleLuraHelper.toc</files>
  <read_first>
    - C:\Users\jonat\Repositories\TerribleBuffTracker\TerribleBuffTracker.toc (verbatim shape — 19 lines)
    - .planning/REQUIREMENTS.md SCAF-01
    - .planning/phases/01-scaffolding-foundation/01-CONTEXT.md D-01, D-02, D-09
  </read_first>
  <action>
Create `TerribleLuraHelper.toc` at the repo root. Mirror TBT's structure (read TBT's .toc first), with these exact differences:

1. `## Title:` is `TerribleLuraHelper` (not TerribleBuffTracker)
2. `## Notes:` is `Helper for L'ura raid encounter — surfaces rune sequence in chat and a smile-arc helper window during boss-fight chat lockdown`
3. `## Category:` is `Raid Tools` (not Buffs & Debuffs, Combat — TLH is encounter-specific)
4. `## X-Curse-Project-ID:` is `1529832` (per D-09)
5. `## X-Wago-ID:` is `XKqArdKy` (per D-09)
6. NO `## IconTexture:` line — D-13/deferred says no .blp icon for v1
7. `## SavedVariables:` is `TerribleLuraHelperDB`
8. URL is `https://github.com/Jonathas-Conceicao/wowTerribleLuraHelper.git`
9. File load list (after the blank line, per D-02 load order — Core first):
   - Core.lua
   - Macros.lua
   - Window.lua
   - Config.lua
   (NO `.xml` files; TLH has no XML templates in v1.)

Final exact content (copy this verbatim):

    ## Interface: 120005
    ## Title: TerribleLuraHelper
    ## Notes: Helper for L'ura raid encounter — surfaces rune sequence in chat and a smile-arc helper window during boss-fight chat lockdown
    ## Author: Jonathas-Conceicao
    ## Version: @project-version@
    ## URL: https://github.com/Jonathas-Conceicao/wowTerribleLuraHelper.git
    ## Category: Raid Tools
    ## X-Curse-Project-ID: 1529832
    ## X-Wago-ID: XKqArdKy
    ## SavedVariables: TerribleLuraHelperDB

    Core.lua
    Macros.lua
    Window.lua
    Config.lua

Note the exact em-dash (`—`, U+2014) in the Notes line.

Note on TOC validation: we do not parse the .toc file with a YAML/structured parser. Every required structural element has its own grep-check below — that's sufficient and avoids a Python/PyYAML availability footgun on the executor's PATH.
  </action>
  <verify>
    <automated>test -f TerribleLuraHelper.toc &amp;&amp; grep -qx "## Interface: 120005" TerribleLuraHelper.toc &amp;&amp; grep -qx "## SavedVariables: TerribleLuraHelperDB" TerribleLuraHelper.toc &amp;&amp; grep -qx "## X-Curse-Project-ID: 1529832" TerribleLuraHelper.toc &amp;&amp; grep -qx "## X-Wago-ID: XKqArdKy" TerribleLuraHelper.toc &amp;&amp; grep -qx "Core.lua" TerribleLuraHelper.toc &amp;&amp; grep -qx "Macros.lua" TerribleLuraHelper.toc &amp;&amp; grep -qx "Window.lua" TerribleLuraHelper.toc &amp;&amp; grep -qx "Config.lua" TerribleLuraHelper.toc &amp;&amp; awk '/^Core\.lua$|^Macros\.lua$|^Window\.lua$|^Config\.lua$/' TerribleLuraHelper.toc | tr '\n' ' ' | grep -qx 'Core.lua Macros.lua Window.lua Config.lua ' &amp;&amp; ! grep -q '^## IconTexture:' TerribleLuraHelper.toc</automated>
  </verify>
  <acceptance_criteria>
    - File `TerribleLuraHelper.toc` exists at repo root
    - Line `## Interface: 120005` is present (exact)
    - Line `## SavedVariables: TerribleLuraHelperDB` is present (exact)
    - Line `## X-Curse-Project-ID: 1529832` is present (exact)
    - Line `## X-Wago-ID: XKqArdKy` is present (exact)
    - Line `## Author: Jonathas-Conceicao` is present
    - Line `## Version: @project-version@` is present (BigWigs Packager substitution token)
    - File contains exactly four bare-line file references in order: `Core.lua`, `Macros.lua`, `Window.lua`, `Config.lua`
    - Load order is enforced (D-02): the four bare-line Lua filenames appear in the exact order `Core.lua Macros.lua Window.lua Config.lua` — verified by `awk '/^Core\.lua$|^Macros\.lua$|^Window\.lua$|^Config\.lua$/' TerribleLuraHelper.toc | tr '\n' ' ' | grep -qx 'Core.lua Macros.lua Window.lua Config.lua '`. A future reordering (e.g. moving Macros.lua before Core.lua) MUST cause this check to fail.
    - No `## IconTexture:` directive line (TLH has no .blp in v1) — verified by `! grep -q '^## IconTexture:' TerribleLuraHelper.toc` (anchored to the TOC directive prefix so a comment mentioning "IconTexture" elsewhere in the file would not falsely fail)
    - No XML files referenced
    - We do not run a structured TOC/JSON/YAML parser — every required field has its own grep above (avoids Python/PyYAML availability dependency)
  </acceptance_criteria>
  <done>The .toc manifest is correct and complete; WoW will recognize the addon and attempt to load the four listed Lua files in order.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write Core.lua with namespace, DB init, dispatcher, banner</name>
  <files>Core.lua</files>
  <read_first>
    - C:\Users\jonat\Repositories\TerribleBuffTracker\Core.lua (lines 1-90 — namespace, ADDON_LOADED handler, DB init pattern, backfill loop, load banner)
    - .planning/REQUIREMENTS.md SCAF-02, SCAF-03
    - .planning/phases/01-scaffolding-foundation/01-CONTEXT.md D-01, D-03, D-04, D-05
    - .planning/PROJECT.md "Hard Constraints" section (Core.lua MUST NOT register chat events, MUST NOT do anything that would touch msg or call SendChatMessage — those land in Phase 2)
  </read_first>
  <action>
Write `Core.lua` with the following structure (use this content as the canonical specification — adapt minor stylua-driven formatting only):

1. Line 1: `local addonName, ns = ...` (mandatory namespace pattern; D-01)

2. Module header comment (Claude's discretion per CONTEXT.md — keep it short, ~5 lines describing Core.lua's role and surfacing the taint constraints):

       -- Core.lua — namespace, SavedVariables init, event dispatcher.
       -- Phase 1: ADDON_LOADED initializes TerribleLuraHelperDB defaults and calls
       -- ns:InitMacros / ns:InitWindow / ns:InitConfig (Phase 2 and Phase 3 implement those).
       -- See CLAUDE.md for hard taint constraints — this file MUST NOT register chat
       -- events, index msg from CHAT_MSG_*, or call SendChatMessage. Those live elsewhere.

3. Event frame registering ONLY `ADDON_LOADED` for Phase 1 (Phase 2 will add PLAYER_REGEN_*, etc.):

       local eventFrame = CreateFrame("Frame")
       eventFrame:RegisterEvent("ADDON_LOADED")

4. OnEvent handler with ONLY the ADDON_LOADED branch:

   - Guard: `if name ~= addonName then return end`

   - Default DB init block — exactly the schema from D-03:

         if not TerribleLuraHelperDB then
             TerribleLuraHelperDB = {
                 enabled = false,
                 listenChannels = {
                     SAY = true,
                     RAID = true,
                     RAID_LEADER = true,
                     RAID_WARNING = true,
                     INSTANCE_CHAT = true,
                     INSTANCE_CHAT_LEADER = true,
                 },
                 window = {
                     scale = 1.00,
                     locked = true,
                     autoHide = false,
                     position = nil,
                 },
                 sequence = {},
             }
         end
         ns.db = TerribleLuraHelperDB

   - Backfill loop — defensive backfill for users with older DBs (mirrors TBT pattern):

         local db = ns.db
         if db.enabled == nil then db.enabled = false end
         if not db.listenChannels then db.listenChannels = {} end
         for _, ch in ipairs({ "SAY", "RAID", "RAID_LEADER", "RAID_WARNING", "INSTANCE_CHAT", "INSTANCE_CHAT_LEADER" }) do
             if db.listenChannels[ch] == nil then db.listenChannels[ch] = true end
         end
         if not db.window then db.window = {} end
         if db.window.scale == nil then db.window.scale = 1.00 end
         if db.window.locked == nil then db.window.locked = true end
         if db.window.autoHide == nil then db.window.autoHide = false end
         -- db.window.position is allowed to be nil (means "use default anchor"); do NOT default it.
         if not db.sequence then db.sequence = {} end

     Note: `position` is intentionally NOT backfilled — nil is a valid state meaning "no saved position; use default anchor on first show". Phase 2 will write to it via OnDragStop.

   - Dispatcher — call the three Init functions if they exist (defensive; the stubs from Task 3 will define them, so all three calls fire in Phase 1):

         if ns.InitMacros then ns:InitMacros() end
         if ns.InitWindow then ns:InitWindow() end
         if ns.InitConfig then ns:InitConfig() end

   - Load banner — colored print on first load. Claude's discretion on color per CONTEXT.md; using `|cffaa44ff` purple/violet to match the POC's color and the Midnight setting:

         print("|cffaa44ffTerribleLuraHelper|r loaded.")

   - Unregister ADDON_LOADED after first match (mirrors TBT — saves CPU):

         self:UnregisterEvent("ADDON_LOADED")

5. NO chat event registration, NO PLAYER_REGEN_* events, NO slash command registration in Phase 1. Those land in Phase 2.

After writing, run `stylua Core.lua` and confirm exit code 0 (no formatting changes needed). If stylua isn't installed, document in the SUMMARY that it must be installed, but do not block the task.
  </action>
  <verify>
    <automated>test -f Core.lua &amp;&amp; head -1 Core.lua | grep -qx "local addonName, ns = \.\.\." &amp;&amp; grep -q "if not TerribleLuraHelperDB then" Core.lua &amp;&amp; grep -q "listenChannels" Core.lua &amp;&amp; grep -q "INSTANCE_CHAT_LEADER = true" Core.lua &amp;&amp; grep -q "scale = 1\.00" Core.lua &amp;&amp; grep -q "locked = true" Core.lua &amp;&amp; grep -q "autoHide = false" Core.lua &amp;&amp; grep -q "sequence = {}" Core.lua &amp;&amp; grep -q "ns:InitMacros" Core.lua &amp;&amp; grep -q "ns:InitWindow" Core.lua &amp;&amp; grep -q "ns:InitConfig" Core.lua &amp;&amp; grep -q "TerribleLuraHelper.*loaded" Core.lua &amp;&amp; ! grep -q "SendChatMessage" Core.lua &amp;&amp; ! grep -q "CHAT_MSG_" Core.lua</automated>
  </verify>
  <acceptance_criteria>
    - File `Core.lua` exists at repo root
    - Line 1 matches the regex `^local addonName, ns = \.\.\.$` (D-01 namespace, SCAF-02)
    - File contains the literal string `if not TerribleLuraHelperDB then` (DB-init guard, SCAF-03)
    - File initializes `enabled = false`, all six `listenChannels.*` entries to `true`, `window.scale = 1.00`, `window.locked = true`, `window.autoHide = false`, and `sequence = {}` (each verified by grep)
    - File explicitly does NOT default `db.window.position` to a non-nil value (the backfill skips it; verify by `! grep -E "window\.position\s*=\s*\{" Core.lua` — no table-literal assignment)
    - File contains all three dispatcher calls: `ns:InitMacros`, `ns:InitWindow`, `ns:InitConfig`
    - File prints a colored load banner whose visible text contains `TerribleLuraHelper` and `loaded`
    - File does NOT contain `SendChatMessage` (taint constraint)
    - File does NOT contain any `CHAT_MSG_` event registration (Phase 2's job)
    - `stylua Core.lua` exits 0 with no diff (or, if stylua absent, run `luac -p Core.lua` if available as a parse-only sanity check)
  </acceptance_criteria>
  <done>Core.lua is the working brain of the addon for Phase 1: namespace set up, DB initialized to the exact D-03 schema with backfill, three Init dispatchers fire on ADDON_LOADED, and the load banner prints. Phase 2 will hang chat-event registration off this file's existing event frame.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write Macros.lua, Window.lua, Config.lua stubs and commit the addon scaffolding</name>
  <files>Macros.lua, Window.lua, Config.lua</files>
  <read_first>
    - .planning/phases/01-scaffolding-foundation/01-CONTEXT.md D-01 (the 4-file split + Init function naming)
    - .planning/REQUIREMENTS.md SCAF-02 (namespace pattern)
    - C:\Users\jonat\Repositories\WeakerScripts\Samples\LuraPatternHelper.lua (skim the module-header docstring — informs the per-file header tone)
    - .planning/PROJECT.md "Hard Constraints" (these stubs MUST be empty enough that nothing in them violates the constraints)
  </read_first>
  <action>
Create three stub Lua files. Each has the same skeleton: `local addonName, ns = ...` first line, a 3-5 line module header comment, and a single empty Init function attached to the `ns` table that Core.lua will dispatch to.

`Macros.lua` content (Phase 2 fills the function body):

    local addonName, ns = ...

    -- Macros.lua — TLH_* macro registration with combat-lockdown deferral.
    -- Phase 2 fills this with: 5 named player macros (TLH_Diamond/Triangle/Circle/Cross/T)
    -- using Blizzard built-in raid-marker FileDataIDs, idempotent on every login,
    -- guarded by InCombatLockdown() with a PLAYER_REGEN_ENABLED retry path.

    function ns:InitMacros()
        -- Phase 1 stub — Phase 2 implementation lands here.
    end

`Window.lua` content (Phase 2 fills):

    local addonName, ns = ...

    -- Window.lua — smile-arc helper window: 5 positional rune slots, BOSS/TANK labels,
    -- lock/unlock, drag-position persistence, 15s self-clear, sequence persistence.
    -- Phase 2 fills this. Slot text MUST be set directly from
    -- C_ChatInfo.ReplaceIconAndGroupExpressions(msg, nil, false) — never index msg in Lua.

    function ns:InitWindow()
        -- Phase 1 stub — Phase 2 implementation lands here.
    end

`Config.lua` content (Phase 3 fills):

    local addonName, ns = ...

    -- Config.lua — Options > AddOns > TerribleLuraHelper panel via the modern Settings.* API.
    -- Phase 3 fills this with: per-channel checkboxes, scale slider, auto-hide toggle,
    -- Recreate Macros / Lock-Window buttons, and a /lura command help block.
    -- Registration is gated on EventUtil.ContinueOnAddOnLoaded; setting bindings use
    -- Settings.RegisterAddOnSetting (the API auto-writes back to TerribleLuraHelperDB).
    -- See .planning/research/SETTINGS_API.md for verified code patterns.

    function ns:InitConfig()
        -- Phase 1 stub — Phase 3 implementation lands here.
    end

After writing all three, run `stylua Macros.lua Window.lua Config.lua` and confirm exit code 0 with no changes. If stylua reformats anything, accept its output (the canonical file content) — but the TLH content above already conforms to stylua defaults (4-space indent, no trailing whitespace, double quotes acceptable).

NO other code in any of these files. No event registration, no slash command setup, no frame creation, no API calls. They exist only to:

1. Establish the per-file namespace (SCAF-02)
2. Define the `ns:Init<X>()` symbol that Core.lua's dispatcher calls
3. Document the file's planned scope for the Phase 2/3 executor

Then commit the addon scaffolding (TOC + 4 Lua files together — they're a single logical unit and the addon doesn't load with any of them missing):

    git add TerribleLuraHelper.toc Core.lua Macros.lua Window.lua Config.lua
    git commit -m "feat(01): addon scaffolding — TOC manifest, Core.lua DB init, stub modules (SCAF-01, SCAF-02, SCAF-03)"
  </action>
  <verify>
    <automated>for f in Macros.lua Window.lua Config.lua; do test -f "$f" || exit 1; head -1 "$f" | grep -qx "local addonName, ns = \.\.\." || exit 1; if grep -q "SendChatMessage" "$f"; then exit 1; fi; if grep -q "CHAT_MSG_" "$f"; then exit 1; fi; done; grep -q "function ns:InitMacros" Macros.lua &amp;&amp; grep -q "function ns:InitWindow" Window.lua &amp;&amp; grep -q "function ns:InitConfig" Config.lua &amp;&amp; git log --oneline -1 | grep -q "addon scaffolding"</automated>
  </verify>
  <acceptance_criteria>
    - All three files exist at repo root
    - Each file's line 1 is exactly `local addonName, ns = ...`
    - `Macros.lua` defines `function ns:InitMacros()` (function with empty body OK)
    - `Window.lua` defines `function ns:InitWindow()` (function with empty body OK)
    - `Config.lua` defines `function ns:InitConfig()` (function with empty body OK)
    - None of the three files contains `SendChatMessage` (taint constraint) — negative checks use `if grep -q PATTERN "$f"; then exit 1; fi` (explicit if/then form is robust under `set -e` and future refactors; do not use `&& exit 1` short-circuit)
    - None of the three files contains `CHAT_MSG_` (chat event registration is Phase 2's job, not Phase 1's) — same `if/then/exit 1` form
    - `stylua` exits 0 on all three files with no diff (or, if stylua absent, files are well-formed Lua: `luac -p Macros.lua Window.lua Config.lua`)
    - Commit on `milestone/0.1.0` includes all five new addon files (TOC + 4 Lua), checkable via `git log --name-only -1`
    - At end: `git log --oneline main..milestone/0.1.0` shows at least 4 commits (3 from Plan 01 + 1 from Plan 02 Task 3)
  </acceptance_criteria>
  <done>The addon code skeleton is complete. WoW can now load TerribleLuraHelper as a no-op addon: TOC parses, Core.lua initializes the DB and dispatches to the three Init stubs, and the load banner prints. Phase 2 fills Macros.lua + Window.lua function bodies; Phase 3 fills Config.lua. No further structural work needed in Phase 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client to addon | The only inbound boundary at Phase 1 is the WoW Lua VM loading the four files via the .toc. No untrusted input crosses this boundary at Phase 1 (Phase 2 introduces the chat-event boundary, which has its own threat model in Phase 2 plans). |
| addon to TerribleLuraHelperDB | Outbound: the addon writes the default schema and reads existing DB on `ADDON_LOADED`. The DB is account-scoped, written to disk by WoW; no other code path touches it in Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | TerribleLuraHelperDB schema corruption from older addon versions | mitigate | Backfill loop in Core.lua's ADDON_LOADED handler defensively re-creates any missing top-level keys (`enabled`, `listenChannels`, `window`, `sequence`) and missing channel/window sub-keys before any consumer reads them. Pattern mirrors TBT's proven approach (Core.lua lines 27-83). |
| T-01-05 | Denial of Service | Malformed .toc preventing load | mitigate | TOC content matches TBT's verified shape, with only literal-value differences (Title, Curse/Wago IDs, Lua file list). The four Lua files referenced are all created in this plan, eliminating the missing-file warning class. |
| T-01-06 | Spoofing | Wrong addonName guard letting another addon's ADDON_LOADED trigger DB init | mitigate | Core.lua's handler enforces `if name ~= addonName then return end` before any DB write — same guard TBT uses. `addonName` is provided by WoW from the TOC's Title; no user input. |
| T-01-07 | Information Disclosure | Banner print leaking sensitive state | accept | Banner text is a fixed string ("TerribleLuraHelper loaded.") — no user state, no DB contents, no IDs. |
| T-01-08 | Elevation of Privilege | Stub Init functions accidentally containing taint-violating code | mitigate | Acceptance criteria explicitly grep-check `if grep -q SendChatMessage; then exit 1; fi` and `if grep -q CHAT_MSG_; then exit 1; fi` on Macros.lua, Window.lua, Config.lua. Stub bodies are empty comments only — no API calls. Phase 2's threat model will cover the chat-event surface when those functions get bodies. |
</threat_model>

<verification>
At end of plan, after Task 3 commits:

    test -f TerribleLuraHelper.toc
    test -f Core.lua && test -f Macros.lua && test -f Window.lua && test -f Config.lua
    head -1 Core.lua && head -1 Macros.lua && head -1 Window.lua && head -1 Config.lua  # all four print "local addonName, ns = ..."
    grep -q "## Interface: 120005" TerribleLuraHelper.toc
    grep -q "if not TerribleLuraHelperDB then" Core.lua
    grep -q "INSTANCE_CHAT_LEADER = true" Core.lua  # confirms grouped schema D-03
    awk '/^Core\.lua$|^Macros\.lua$|^Window\.lua$|^Config\.lua$/' TerribleLuraHelper.toc | tr '\n' ' ' | grep -qx 'Core.lua Macros.lua Window.lua Config.lua '  # D-02 load order
    git log --oneline main..milestone/0.1.0 | head -10  # shows commits accumulated so far on milestone branch
</verification>

<success_criteria>
- TerribleLuraHelper.toc exists with all required `## ` headers including the Curse/Wago IDs from D-09
- TerribleLuraHelper.toc lists the four Lua files in the exact D-02 order (verified by the awk-based order check)
- Core.lua initializes TerribleLuraHelperDB to the exact D-03 grouped schema with backfill
- Three stub modules (Macros/Window/Config) have correct namespace and Init function signatures
- All four Lua files pass stylua with default config (or, if stylua not installed, parse cleanly)
- Phase goal "addon loads as a no-op and prints its load banner" is satisfied — once installed, WoW will load it without errors and TerribleLuraHelperDB will populate after `/reload`
- Plan 03 (release pipeline) can finalize independently in the same wave because it does not touch any of these files
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffolding-foundation/01-02-SUMMARY.md` documenting:
- Files created with line counts
- D-03 schema confirmation (paste the actual `TerribleLuraHelperDB = { ... }` block from Core.lua)
- stylua run output (exit code; any reformatting applied)
- Commit hash and oneline message
- Smoke-test note: "Run scripts/install.bat (after Plan 03 lands it) + /reload in WoW to verify the load banner prints and TerribleLuraHelperDB appears in SavedVariables. End-to-end smoke test belongs to phase verification, not this plan."
</output>
</content>
</invoke>